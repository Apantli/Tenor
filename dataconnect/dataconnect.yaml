specVersion: "v1beta"
serviceId: "tenor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tenor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
